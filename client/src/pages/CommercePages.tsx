import { ArrowLeft, ArrowRight, Check, Heart, Minus, Plus, ShieldCheck, ShoppingBag, Trash2, UserRound } from "lucide-react";
import { Link } from "wouter";
import { Footer, formatPrice, ProductArtwork, ProductCard, SiteHeader } from "@/components/storefront";
import { products } from "@/lib/store";
import { useStorefront } from "@/contexts/StorefrontContext";
import { toast } from "sonner";

export function Cart() {
  const { cart, subtotal, updateQuantity, removeItem } = useStorefront();
  const shipping = subtotal >= 100 || subtotal === 0 ? 0 : 8;
  const total = subtotal + shipping;
  return <div className="min-h-screen bg-white text-slate-950"><SiteHeader /><main className="mx-auto max-w-7xl px-5 py-12 md:px-8 md:py-20"><Link href="/shop" className="inline-flex items-center gap-2 text-xs font-bold text-slate-500 hover:text-slate-950"><ArrowLeft size={14} /> Continue shopping</Link><div className="mt-8 grid gap-12 lg:grid-cols-[1fr_23rem]"><div><div className="flex items-end justify-between border-b border-slate-200 pb-6"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-violet-600">Your selection</p><h1 className="mt-3 text-5xl font-black tracking-[-0.08em]">The bag.</h1></div><span className="text-sm text-slate-500">{cart.length} {cart.length === 1 ? "object" : "objects"}</span></div>{cart.length ? <div className="divide-y divide-slate-200">{cart.map((line) => <div key={line.product.id} className="flex gap-4 py-6 sm:gap-6"><div className="w-28 shrink-0 sm:w-36"><ProductArtwork product={line.product} /></div><div className="flex min-w-0 flex-1 flex-col justify-between gap-4 sm:flex-row"><div><Link href={`/products/${line.product.id}`} className="text-base font-bold hover:text-violet-700">{line.product.name}</Link><p className="mt-1 text-xs text-slate-500">{line.product.categoryLabel}</p><button onClick={() => removeItem(line.product.id)} className="mt-5 inline-flex items-center gap-1.5 text-xs font-semibold text-slate-400 hover:text-rose-600"><Trash2 size={13} /> Remove</button></div><div className="flex items-center justify-between gap-6 sm:flex-col sm:items-end"><p className="text-sm font-bold">{formatPrice(line.product.price * line.quantity)}</p><div className="flex items-center gap-1 rounded-full border border-slate-200 px-1 py-1"><button aria-label="Decrease quantity" onClick={() => updateQuantity(line.product.id, line.quantity - 1)} className="flex h-7 w-7 items-center justify-center rounded-full hover:bg-slate-100"><Minus size={13} /></button><span className="w-6 text-center text-xs font-bold">{line.quantity}</span><button aria-label="Increase quantity" onClick={() => updateQuantity(line.product.id, line.quantity + 1)} className="flex h-7 w-7 items-center justify-center rounded-full hover:bg-slate-100"><Plus size={13} /></button></div></div></div></div>)}</div> : <div className="flex min-h-80 flex-col items-center justify-center rounded-[2rem] bg-slate-50 text-center"><ShoppingBag className="text-slate-400" size={26} /><h2 className="mt-4 text-xl font-black tracking-[-0.04em]">Your bag is feeling light.</h2><p className="mt-2 text-sm text-slate-500">Save something you love, then it will be right here.</p><Link href="/shop" className="mt-5 rounded-full bg-slate-950 px-5 py-3 text-xs font-bold text-white">Browse the edit</Link></div>}</div><aside className="h-fit rounded-[1.8rem] bg-slate-50 p-6 md:p-7"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-slate-400">Order summary</p><div className="mt-6 grid gap-4 border-b border-slate-200 pb-6 text-sm"><div className="flex justify-between"><span className="text-slate-500">Subtotal</span><span className="font-semibold">{formatPrice(subtotal)}</span></div><div className="flex justify-between"><span className="text-slate-500">Delivery</span><span className="font-semibold">{shipping === 0 ? "Complimentary" : formatPrice(shipping)}</span></div></div><div className="flex justify-between py-5 text-base font-black"><span>Total</span><span>{formatPrice(total)}</span></div><Link href={cart.length ? "/checkout" : "/shop"} className="flex w-full items-center justify-center gap-2 rounded-full bg-slate-950 px-5 py-4 text-sm font-bold text-white transition hover:bg-violet-700">{cart.length ? "Continue to checkout" : "Start shopping"} <ArrowRight size={16} /></Link><p className="mt-4 text-center text-[11px] leading-5 text-slate-400">Taxes calculated at checkout. Free delivery on orders over $100.</p><div className="mt-7 flex gap-3 border-t border-slate-200 pt-5"><ShieldCheck className="shrink-0 text-teal-600" size={17} /><p className="text-xs leading-5 text-slate-500">Secure checkout, thoughtful packaging, and a 30-day return window.</p></div></aside></div></main><Footer /></div>;
}

export function Wishlist() {
  const { wishlist, toggleSaved, addItem } = useStorefront();
  const savedProducts = products.filter((product) => wishlist.includes(product.id));
  return <div className="min-h-screen bg-white text-slate-950"><SiteHeader /><main className="mx-auto max-w-7xl px-5 py-12 md:px-8 md:py-20"><div className="max-w-xl"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-violet-600">Your saved edit</p><h1 className="mt-4 text-5xl font-black tracking-[-0.08em] md:text-7xl">The wishlist.</h1><p className="mt-4 text-base leading-7 text-slate-500">Keep the pieces you’re thinking about close by.</p></div>{savedProducts.length ? <div className="mt-12 grid gap-x-5 gap-y-12 sm:grid-cols-2 lg:grid-cols-4">{savedProducts.map((product) => <div key={product.id} className="group"><div className="relative"><Link href={`/products/${product.id}`}><ProductArtwork product={product} /></Link><button onClick={() => toggleSaved(product.id)} aria-label={`Remove ${product.name} from wishlist`} className="absolute right-4 top-4 flex h-10 w-10 items-center justify-center rounded-full bg-white/80 text-violet-700 shadow-sm backdrop-blur"><Heart size={16} fill="currentColor" /></button></div><div className="mt-4 flex items-start justify-between gap-3"><div><Link href={`/products/${product.id}`} className="text-[15px] font-bold hover:text-violet-700">{product.name}</Link><p className="mt-1 text-xs text-slate-500">{product.categoryLabel}</p></div><p className="text-sm font-bold">{formatPrice(product.price)}</p></div><button onClick={() => { addItem(product); toggleSaved(product.id); toast.success("Moved to your bag"); }} className="mt-4 w-full rounded-full border border-slate-200 px-4 py-3 text-xs font-bold transition hover:border-slate-950">Move to bag</button></div>)}</div> : <div className="mt-12 flex min-h-80 flex-col items-center justify-center rounded-[2rem] bg-gradient-to-br from-violet-50 to-teal-50 text-center"><Heart className="text-violet-500" size={26} /><h2 className="mt-4 text-xl font-black tracking-[-0.04em]">Nothing saved yet.</h2><p className="mt-2 text-sm text-slate-500">Tap the heart on anything that catches your eye.</p><Link href="/shop" className="mt-5 rounded-full bg-slate-950 px-5 py-3 text-xs font-bold text-white">Explore the edit</Link></div>}</main><Footer /></div>;
}

export function Account() {
  return <div className="min-h-screen bg-white text-slate-950"><SiteHeader /><main className="mx-auto max-w-7xl px-5 py-12 md:px-8 md:py-20"><div className="grid gap-10 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-violet-600">Your atelier</p><h1 className="mt-4 text-5xl font-black leading-[0.95] tracking-[-0.08em]">A quieter way<br />to keep track.</h1><p className="mt-5 max-w-sm text-base leading-7 text-slate-500">Sign in to see orders, save addresses, and keep your favorite objects together.</p><button onClick={() => toast.info("Authentication is ready to connect", { description: "This entry point is prepared for your preferred auth provider." })} className="mt-8 inline-flex items-center gap-2 rounded-full bg-slate-950 px-6 py-4 text-sm font-bold text-white transition hover:bg-violet-700">Continue with account <ArrowRight size={16} /></button></div><div className="grid gap-4 sm:grid-cols-2"><div className="rounded-[1.6rem] bg-violet-50 p-6"><UserRound className="text-violet-700" size={20} /><h2 className="mt-16 text-xl font-black tracking-[-0.04em]">Personal details</h2><p className="mt-2 text-sm leading-6 text-slate-500">Keep your delivery details ready for the next order.</p></div><div className="rounded-[1.6rem] bg-teal-50 p-6"><ShoppingBag className="text-teal-700" size={20} /><h2 className="mt-16 text-xl font-black tracking-[-0.04em]">Order history</h2><p className="mt-2 text-sm leading-6 text-slate-500">Everything you’ve brought home, in one place.</p></div><div className="rounded-[1.6rem] border border-slate-200 p-6 sm:col-span-2"><div className="flex items-center gap-2 text-amber-600"><Check size={18} /><span className="text-xs font-bold uppercase tracking-[0.16em]">Account-ready by design</span></div><p className="mt-4 max-w-lg text-xl font-black leading-tight tracking-[-0.05em]">Your account is the bridge between inspiration today and an easier checkout tomorrow.</p></div></div></div></main><Footer /></div>;
}

export function Checkout() {
  const { subtotal, cart } = useStorefront();
  const total = subtotal + (subtotal >= 100 ? 0 : 8);
  return <div className="min-h-screen bg-[#fbfbfd] text-slate-950"><SiteHeader /><main className="mx-auto max-w-5xl px-5 py-12 md:px-8 md:py-20"><Link href="/cart" className="inline-flex items-center gap-2 text-xs font-bold text-slate-500 hover:text-slate-950"><ArrowLeft size={14} /> Back to bag</Link><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_20rem]"><div className="rounded-[2rem] bg-white p-6 shadow-sm md:p-8"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-violet-100 text-xs font-black text-violet-700">1</span><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-violet-600">Almost yours</p><h1 className="mt-1 text-3xl font-black tracking-[-0.06em]">Checkout</h1></div></div>{cart.length ? <div className="mt-10 grid gap-8"><div><label className="text-xs font-bold text-slate-700">Email address</label><input className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3.5 text-sm outline-none transition focus:border-violet-400" placeholder="you@example.com" /></div><div><div className="flex items-center justify-between"><label className="text-xs font-bold text-slate-700">Delivery address</label><span className="text-[10px] font-bold uppercase tracking-[0.16em] text-slate-400">Secure & private</span></div><div className="mt-2 grid gap-3 sm:grid-cols-2"><input className="rounded-xl border border-slate-200 px-4 py-3.5 text-sm outline-none focus:border-violet-400 sm:col-span-2" placeholder="Full name" /><input className="rounded-xl border border-slate-200 px-4 py-3.5 text-sm outline-none focus:border-violet-400 sm:col-span-2" placeholder="Street address" /><input className="rounded-xl border border-slate-200 px-4 py-3.5 text-sm outline-none focus:border-violet-400" placeholder="City" /><input className="rounded-xl border border-slate-200 px-4 py-3.5 text-sm outline-none focus:border-violet-400" placeholder="Postal code" /></div></div><div><label className="text-xs font-bold text-slate-700">Payment</label><div className="mt-2 rounded-xl border border-slate-200 p-4 text-sm text-slate-500">Demo payment flow · no card details are stored</div></div><button onClick={() => toast.success("Your order is ready to place", { description: "Connect your payment provider to complete the final step." })} className="flex items-center justify-center gap-2 rounded-full bg-slate-950 px-6 py-4 text-sm font-bold text-white transition hover:bg-violet-700">Place demo order <ArrowRight size={16} /></button></div> : <div className="mt-10 rounded-2xl bg-slate-50 p-8 text-center text-sm text-slate-500">Your bag is empty. <Link href="/shop" className="font-bold text-slate-950 underline">Return to shop</Link></div>}</div><aside className="h-fit rounded-[2rem] bg-slate-950 p-6 text-white md:p-7"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-white/45">Your order</p><div className="mt-6 grid gap-4 border-b border-white/10 pb-6">{cart.map((line) => <div key={line.product.id} className="flex justify-between gap-4 text-sm"><span className="text-white/65">{line.product.name} × {line.quantity}</span><span>{formatPrice(line.product.price * line.quantity)}</span></div>)}</div><div className="flex justify-between py-5 text-base font-black"><span>Total</span><span>{formatPrice(total)}</span></div><div className="flex gap-3 rounded-2xl bg-white/10 p-4 text-xs leading-5 text-white/65"><ShieldCheck className="shrink-0 text-teal-300" size={16} /> This is a secure, demo-ready checkout entry point.</div></aside></div></main></div>;
}
